# 票据审核系统 — 前端与部署说明书

对前端组件结构、路由设计、移动端适配、Nginx 部署配置进行详细设计。

依据概要设计说明书，对前端组件结构、路由设计、移动端适配、部署配置进行详细设计。

---
## 8. 前端组件树

```
index.vue (我的票据)
├─ SearchForm.vue   Props: queryParams, showSearch   Events: @query, @reset
├─ BillTable.vue    Props: billList, loading, total   Events: @add, @edit, @delete, @detail, @submit
├─ BillForm.vue     Props: modelValue, title, formData  Events: @update:modelValue, @success
└─ BillDetail.vue   Props: modelValue, billData       Events: @update:modelValue
```

### 8.1 表格列设计

| 列 | prop | 宽度 | 渲染 |
|----|------|------|------|
| 票据编号 | billNo | 180 | show-overflow-tooltip |
| 标题 | title | 200 | show-overflow-tooltip |
| 类别 | categoryName | 100 | text |
| 金额 | amount | 120 | 右对齐 ¥0.00 |
| 状态 | status | 100 | dict-tag（字典 `biz_bill_status`） |
| 创建时间 | createTime | 160 | parseTime |
| 操作 | — | 240 | 动态显示 |

### 8.2 操作列按钮显隐

| 按钮 | 显示条件 | 权限 |
|------|----------|------|
| 详情 | 始终 | v-hasPermi="['biz:bill:query']" |
| 编辑 | status IN ('0','3') | v-hasPermi="['biz:bill:add']" |
| 提交 | status IN ('0','3') | v-hasPermi="['biz:bill:add']" |
| 删除 | status === '0' | v-hasPermi="['biz:bill:remove']" |

### 8.3 表单字段

| 字段 | 组件 | 规则 |
|------|------|------|
| 票据标题 | el-input | required, maxlength=200 |
| 票据类别 | el-select | 选项来自 biz_category 表 |
| 金额 | el-input-number | required, min=0, precision=2 |
| 描述 | el-input textarea | rows=4 |
| 附件 | FileUpload | 多文件上传 |

### 8.4 双按钮逻辑

```javascript
function handleSaveDraft() { form.status = "0"; submitForm(); }
function handleSubmit()   { form.status = "1"; submitForm(); }
```

---

## 9. 错误信息

业务异常消息:

- "管理员不能执行用户操作！"
- "票据不存在！"
- "只能查看自己的票据！"
- "只能编辑草稿或已退回状态的票据！"
- "只能编辑自己的票据！"
- "只能提交草稿或已退回状态的票据！"
- "只能提交自己的票据！"
- "只能删除草稿状态的票据！"
- "只能删除自己的票据！"
- "只能审核已提交状态的票据！"
- "无效的审核结果，action 必须为 1（通过）或 2（退回）！"
- "系统错误，票据保存失败！"
- "系统错误，票据更新失败！"
- "系统错误，票据提交失败！"
- "系统错误，审核失败！"

---

## 10. 文件清单

### 10.1 后端（16 个文件）

```
zsc-module/src/main/java/com/zsc/module/
├── controller/BizBillController.java          ← 待开发
├── domain/
│   ├── entity/BizBill.java
│   ├── entity/BizBillFile.java
│   ├── entity/BizAuditLog.java
│   ├── dto/BizBillDto.java
│   ├── dto/BizBillReviewDto.java
│   ├── dto/query/BizBillQueryDto.java
│   ├── vo/BizBillVo.java
│   ├── vo/BizBillDetailVo.java
│   ├── vo/BizBillFileVo.java
│   └── vo/BizAuditLogVo.java
├── mapper/BizBillMapper.java
├── mapper/BizBillFileMapper.java
├── mapper/BizAuditLogMapper.java
├── service/BizBillService.java
└── service/impl/BizBillServiceImpl.java

zsc_backend/sql/migrations/
├── 02_add_bill_table.sql
├── 03_add_bill_file_table.sql
├── 04_add_audit_log_table.sql
└── 05_seed_bill_dict.sql
```

### 10.2 前端（7 个文件，待开发）

```
zsc_frontend/src/
├── api/biz/bill.js
└── views/biz/bill/
    ├── index.vue
    ├── components/SearchForm.vue
    ├── components/BillTable.vue
    ├── components/BillForm.vue
    ├── components/BillDetail.vue
    └── review/index.vue
```
