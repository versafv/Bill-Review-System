# 票据审核系统 — 详细设计说明书

对核心数据库表结构、种子数据、实体类映射进行详细设计。

---


## 1. 数据库 DDL

### 1.1 biz_bill（票据主表）

```sql
DROP TABLE IF EXISTS biz_bill;
CREATE TABLE biz_bill (
    id            bigint(20)    NOT NULL AUTO_INCREMENT COMMENT '主键',
    bill_no       varchar(32)   NOT NULL COMMENT '票据编号',
    title         varchar(255)  NOT NULL COMMENT '票据标题',
    category_id   bigint(20)    DEFAULT NULL COMMENT '票据类别ID',
    amount        decimal(10,2) NOT NULL COMMENT '金额',
    description   varchar(500)  DEFAULT NULL COMMENT '描述',
    status        char(1)       DEFAULT '0' COMMENT '0-草稿 1-已提交 2-已通过 3-已退回',
    attachment    varchar(500)  DEFAULT NULL COMMENT '附件路径',
    create_by     varchar(64)   DEFAULT NULL COMMENT '创建者',
    create_time   datetime      DEFAULT NULL COMMENT '创建时间',
    update_by     varchar(64)   DEFAULT NULL COMMENT '更新者',
    update_time   datetime      DEFAULT NULL COMMENT '更新时间',
    audit_by      varchar(64)   DEFAULT NULL COMMENT '最近审核人',
    audit_time    datetime      DEFAULT NULL COMMENT '最近审核时间',
    audit_comment varchar(500)  DEFAULT NULL COMMENT '最近审核意见',
    remark        varchar(500)  DEFAULT NULL COMMENT '备注',
    PRIMARY KEY (id),
    UNIQUE KEY uk_bill_no (bill_no),
    KEY idx_create_by (create_by),
    KEY idx_status (status),
    KEY idx_category_id (category_id),
    KEY idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='票据表';
```

### 1.2 biz_bill_file（附件表）

```sql
DROP TABLE IF EXISTS biz_bill_file;
CREATE TABLE biz_bill_file (
    id          bigint(20)   NOT NULL AUTO_INCREMENT COMMENT '主键',
    bill_id     bigint(20)   NOT NULL COMMENT '票据ID',
    file_name   varchar(255) NOT NULL COMMENT '原始文件名',
    file_path   varchar(500) NOT NULL COMMENT '存储路径',
    file_size   bigint(20)   DEFAULT NULL COMMENT '文件大小（字节）',
    file_type   varchar(50)  DEFAULT NULL COMMENT '文件类型',
    sort_order  int(11)      DEFAULT 0 COMMENT '排序',
    create_by   varchar(64)  DEFAULT NULL COMMENT '上传者',
    create_time datetime     DEFAULT NULL COMMENT '上传时间',
    PRIMARY KEY (id),
    KEY idx_bill_id (bill_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='票据附件表';
```

### 1.3 biz_audit_log（审核记录表）

```sql
DROP TABLE IF EXISTS biz_audit_log;
CREATE TABLE biz_audit_log (
    id          bigint(20)   NOT NULL AUTO_INCREMENT COMMENT '主键',
    bill_id     bigint(20)   NOT NULL COMMENT '票据ID',
    action      char(1)      NOT NULL COMMENT '操作: 1-通过 2-退回',
    comment     varchar(500) DEFAULT NULL COMMENT '审核意见',
    audit_by    varchar(64)  NOT NULL COMMENT '审核人',
    audit_time  datetime     NOT NULL COMMENT '审核时间',
    PRIMARY KEY (id),
    KEY idx_bill_id (bill_id),
    KEY idx_audit_by (audit_by),
    KEY idx_audit_time (audit_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='审核记录表';
```

---

## 2. 种子数据

### 2.1 系统角色

```sql
-- 票据审核员 (role_id=4)
INSERT INTO `sys_role` VALUES
(4, '票据审核员', 'reviewer', 4, '1', 1, 1, '0', '0', 'admin', NOW(), '', NULL, '审核通过/退回票据');

-- 系统管理员 (role_id=5)
INSERT INTO `sys_role` VALUES
(5, '系统管理员', 'admin_user', 5, '1', 1, 1, '0', '0', 'admin', NOW(), '', NULL, '管理用户与类别');
```

### 2.2 业务菜单（3000-3018）

```sql
-- 票据管理目录
INSERT INTO `sys_menu` VALUES
(3000, '票据管理', 0, 4, 'bill', NULL, '', '', 1, 0, 'M', '0', '0', '', 'bill', ...),
(3012, '票据概览', 3000, 1, 'manage', 'biz/bill/manage/index', ... 'biz:bill:list', ...),
(3001, '我的票据', 3000, 2, 'myBill', 'biz/bill/index', ... 'biz:bill:list', ...),
(3002, '票据审核', 3000, 3, 'review', 'biz/bill/review/index', ... 'biz:bill:list', ...),
-- 按钮权限
(3003, '票据查询', ... 'biz:bill:query', ...),
(3006, '审核按钮', ... 'biz:bill:review', ...),
(3008, '类别查询', ... 'biz:category:query', ...);

-- 系统管理目录
INSERT INTO `sys_menu` VALUES
(3014, '系统管理', 0, 5, 'admin', NULL, 'M', ...),
(3015, '管理概览', 3014, 1, 'dashboard', 'biz/admin/index', ... 'biz:admin:list', ...),
(3016, '用户管理', 3014, 2, 'users', 'biz/admin/user/index', ... 'system:user:list', ...),
(3017, '类别管理', 3014, 3, 'bizCategory', 'biz/bizCategory/index', ... 'biz:category:list', ...),
(3018, '注册审核', 3014, 4, 'registerReview', 'biz/admin/register/index', ... 'biz:admin:list', ...);
```

### 2.3 注册申请表

```sql
CREATE TABLE biz_register_request (
    id                  bigint(20)   NOT NULL AUTO_INCREMENT,
    email               varchar(50)  NOT NULL COMMENT '申请邮箱',
    note                varchar(500) DEFAULT NULL COMMENT '申请说明',
    role_key            varchar(20)  NOT NULL COMMENT '申请角色: user/reviewer',
    status              char(1)      DEFAULT '0' COMMENT '0-待审 1-通过 2-拒绝',
    generated_username  varchar(30)  DEFAULT NULL COMMENT '审批后生成的用户名',
    generated_password  varchar(100) DEFAULT NULL COMMENT '审批后生成的密码',
    review_by           varchar(64)  DEFAULT NULL COMMENT '审批人',
    review_time         datetime     DEFAULT NULL COMMENT '审批时间',
    review_comment      varchar(500) DEFAULT NULL COMMENT '审批意见',
    create_time         datetime     NOT NULL,
    PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

---

## 3. 实体类设计

### 3.1 BizBill — 票据主表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键 |
| billNo | String | 草稿: DRAFT- + 时间戳; 提交: BILL-YYYYMMDD-NNNN |
| title | String | 票据标题 |
| categoryId | Long | 票据类别ID，关联 biz_category |
| amount | BigDecimal | 金额 |
| description | String | 描述 |
| status | String | 0-草稿 1-已提交 2-已通过 3-已退回 |
| attachment | String | 附件路径 |
| createBy | String | 创建者用户名（RuoYi 约定） |
| createTime | Date | 创建时间 |
| updateBy | String | 更新者 |
| updateTime | Date | 更新时间 |
| auditBy | String | 最近审核人 |
| auditTime | Date | 最近审核时间 |
| auditComment | String | 最近审核意见 |
| remark | String | 备注 |

### 3.2 BizBillFile — 附件表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键 |
| billId | Long | 关联票据ID |
| fileName | String | 原始文件名 |
| filePath | String | 服务器存储路径 |
| fileSize | Long | 文件大小（字节） |
| fileType | String | 文件类型 / 扩展名 |
| sortOrder | Integer | 附件排序 |
| createBy | String | 上传者 |
| createTime | Date | 上传时间 |

### 3.3 BizAuditLog — 审核记录表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键 |
| billId | Long | 关联票据ID |
| action | String | 1-通过 2-退回 |
| comment | String | 审核意见 |
| auditBy | String | 审核人 |
| auditTime | Date | 审核时间 |

---

